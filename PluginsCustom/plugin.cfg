# Embedded plugin manifest (kept as an EmbeddedResource).
# Keep keys one-per-line as simple `key=value` pairs.
# NOTE: The server reads this file from the assembly resources.

name=PluginsCustom
id=pluginscustom
version=1.0.0
author=LovelyAphorist
entry=PluginsCustom.Webfishing2.0
# Optional, displayed by some tooling
description=Starter plugin for WebFishingCove (built from TemplateCovePlugin)
